default: &default
  dispatchers:
    - polling_interval: 1
      batch_size: 100
  workers:
    - queues: "*"
      threads: 2
      polling_interval: 1

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
